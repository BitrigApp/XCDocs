/// Version information for the `XCDocs` package.
@available(macOS 26, *)
public enum Version {
    /// The semantic version string for the current package build.
    public static let string = "0.1.0"
}
